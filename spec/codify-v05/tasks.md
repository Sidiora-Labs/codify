<!-- GENERATED by cg spec render from spec/ kvx — DO NOT EDIT. -->

# Implementation Plan

## Tasks

- [ ] 1. ACP core
  - [x] 1.1 AcpClient — ndjson JSON-RPC over child stdio
    - AcpClient: spawn agent command, newline-delimited JSON-RPC framing, id correlation, request/notify, onRequest/onNotify dispatch
    - initialize handshake helper with protocolVersion 1 and fs capabilities; hard failure on version mismatch or spawn error
    - Process-exit and parse-error handling: reject pending requests, emit close reason
    - Fake ACP agent fixture (node) + integration test driving a full session: initialize, session/new, prompt, streamed updates, permission round-trip, fs read/write, cancel
    - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [ ] 2. Session bridge and panel
  - [x] 2.1 Panel session lifecycle bridged to the board
    - startPanelSession: claim (parallel mode) + spec start + resume-packet first prompt, reusing the agents.js discipline
    - session/new carries workspace cwd and mcpServers: [{name: codify, command: cg, args: [mcp]}]
    - fs/read_text_file and fs/write_text_file served from the workspace; paths outside the root refused
    - On session end without done/implemented: offer handoff and claim release; on qualify: quiet close
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_
  - [x] 2.2 Agent panel webview
    - Webview panel: task header, streaming transcript (message and collapsed thought chunks), tool-call cards with kind/status/diff, plan view
    - Permission requests rendered as option buttons; reply carries the selected optionId; cancelled turns resolve the request as cancelled
    - Input box with send; cancel button while a turn runs; queued input after cancel
    - VS Code theme variables throughout; strict webview CSP with nonce
    - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [ ] 3. Surface
  - [x] 3.1 Commands, settings, wiring, docs
    - Commands: codify.agent.openPanel (task node + palette), panel variants wired beside the terminal commands
    - Settings: codify.acp.claudeCommand, codify.acp.codexCommand, codify.acp.customCommand, codify.agent.interface (panel|terminal)
    - Task-node start honors codify.agent.interface; terminal path stays the fallback when no adapter is available
    - README agent-panel section, vscode README settings table, CHANGELOG entry; 14_vscode manifest coherence covers acp.js
    - _Requirements: 2.1, 3.1_
  - [x] 3.2 Sidebar agent view: chat-first, task optional
    - codifyAgentView webview view in the Codify container — always visible, like the Codex extension's chat, no task required
    - First message spawns the ACP session lazily (cwd session, MCP injected, no claim); driver picker in the header; New Chat resets
    - Start-on-task routes into the sidebar view when idle (claim + resume packet); busy view offers replace-or-open-beside; editor panels remain for concurrent tasks
    - Session end with an attached unqualified task still offers handoff/release; 14_vscode covers the view registration
    - _Requirements: 3.1_
  - [x] 3.3 Full chat surface: markdown, tool cards, and cg slash commands
    - Chat rendering: markdown (headings, lists, tables, quotes), fenced code blocks with copy, collapsed thoughts, and file paths that open in the editor
    - Slash palette over the cg verbs (brief, next, review, check, context, search, impact, why, …): the output lands as a card and is handed to the agent as context
    - Composer: autosizing input, history recall, queued input while a turn runs, stop, and a context bar carrying feature, task chip, task status, and driver
    - Panel script driven headlessly against a DOM shim in 19_acp.sh: rendering, tool/permission/plan cards, palette, and the ready handshake
    - _Requirements: 3.1, 3.2, 3.3, 3.4_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1"] },
    { "id": 2,  "tasks": ["2.1", "2.2"] },
    { "id": 3,  "tasks": ["3.1", "3.2", "3.3"] }
  ]
}
```
