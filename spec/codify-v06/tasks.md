<!-- GENERATED by cg spec render from spec/ kvx — DO NOT EDIT. -->

# Implementation Plan

## Tasks

- [ ] 1. Capture
  - [x] 1.1 Comment spans, classification, and the anchor index
    - clean_line reports the comment byte range it just blanked (cmt_mark); lang_parse coalesces consecutive pure-comment lines into one span (cmt_flush/add_cmt). No second pass over file bytes.
    - comments table (file_id, line, end_line, kind, sym_id, body, anchored_hash) plus comment_fts over body keyed by comments.id; delete-and-reinsert per file like body_fts. stale is derived in task 4.1, not stored.
    - Classify by position against the def list the same pass already builds: doc when the span ends on the line before a def, file when it precedes every def, inline when inside a symbol extent, orphan otherwise
    - Bind doc and inline comments to sym_id; reuse lang_scope_end extents so binding matches the scope attribution v0.4 already proved
    - Unit tests for classification across brace, indentation, and block-comment languages; 20_anchors.sh fixture repo covering c, go, python, ts
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6, 1.7_

- [ ] 2. Retrieval
  - [x] 2.1 Doc-first snippets
    - symbol_doc: fetch the bound doc comment for a symbol, returning NULL when absent or when it fails the derivability check
    - file_snippet leads with doc plus signature when a doc exists; unchanged body-line fallback when it does not
    - Stale docs render with a stale marker; over-long docs truncate with an announced cut
    - Budget accounting unchanged in shape: docs count against the same cap, so more symbols survive the same budget
    - 16_retrieval asserts the symbol-count increase for a fixed query and budget against the body-first baseline
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

- [ ] 3. Edges
  - [x] 3.1 Soft edges from anchor prose
    - Tokenize identifier-shaped words, dotted names, and repository-relative paths out of captured comment text
    - Record them into refs with kind soft, file_id and line of the comment, sym_id of the bound symbol; resolution stays by name at query time, matching the existing refs contract
    - Drop tokens matching no symbol, file, or route: silence beats an invented edge
    - callers_of, callees_of, and cg impact label soft edges distinctly; --no-soft excludes them
    - Accuracy test proves soft edges never displace or overwrite a parsed call edge
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_

- [ ] 4. Honesty and survey
  - [x] 4.1 Anchor drift baseline, guard, and check
    - anchored_hash carries the bound symbol's body hash as of the last time the comment text itself changed: on reindex, unchanged comment text keeps the old baseline, changed text re-baselines
    - stale is derived, not stored by hand: current body hash differs from anchored_hash
    - Retrieval ranking pushes stale anchors below current ones wherever anchors are ranked
    - cg guard reports anchors the uncommitted work made stale, as warnings only, per the warn-dont-block rule
    - cg check reports the stale anchor count in its summary
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_
  - [x] 4.2 cg survey — the tier below bodies
    - cg survey [path|query] [--budget N] [--json]: file purpose lines, then symbol docs with signatures, grouped by file. Never a body.
    - Per-file cost bounded so one hundred files fit the default budget; entries cut by the budget reported as an explicit omitted count
    - Files and symbols carrying no anchor listed under uncovered rather than dropped
    - MCP tool exposing the same output; registered beside context and impact
    - 20_anchors asserts the no-bodies guarantee and the hundred-file bound
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 5. Surface
  - [x] 5.1 cg anchors, the convention, and the docs
    - cg anchors [--stale] [--uncovered] [--json]: stale anchors and unanchored symbols ranked by coordination score, so backfill starts at the orchestration points rather than at sb_puts
    - Document the four kinds — purpose, contract, danger, pointer — with the derivability test: if an agent could have written the comment by reading the code, it is not an anchor
    - workflow.kvx gains anchors in the loop; regenerate AGENTS.md and CLAUDE.md through specgen rather than editing them
    - README intent-layer section, CHANGELOG entry for 0.6.0
    - Backfill guidance: cg anchors --uncovered is the work list, and the drift lint is what keeps a backfill honest afterwards
    - _Requirements: 6.1, 6.2, 6.3, 6.4_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1"] },
    { "id": 2,  "tasks": ["2.1", "3.1"] },
    { "id": 3,  "tasks": ["4.1", "4.2"] },
    { "id": 4,  "tasks": ["5.1"] }
  ]
}
```
