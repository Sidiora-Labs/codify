<!-- GENERATED by cg spec render from spec/ kvx — DO NOT EDIT. -->

# Implementation Plan: Codify v0.3

## Tasks

## Wave 0: correct boundaries

- [ ] 0. Foundation
  - [x] 0.1 Bound root resolution and harden the database
    - Stop the upward walk at .git/.hg/.svn, $HOME, and mount boundaries
    - Add cg_find_root_at for explicit-directory resolution
    - init checks cwd only; --nested for ancestors; refuse $HOME without --force
    - Add cg root and print the project root in cg info
    - Enable WAL and a busy timeout in cg_open
    - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_
  - [x] 0.2 Honour .gitignore
    - Parse .gitignore with negation and anchored patterns alongside .cgignore
    - _Requirements: 3.1_

## Wave 1: make context worth the call

- [ ] 1. Graph quality
  - [x] 1.1 Tokenized search and query-aware context
    - Split queries into tokens, match each, fuse scores
    - Derive entry points from the query: routes, exported symbols in matched files, then main()
    - _Requirements: 2.1, 2.2_
  - [x] 1.2 Symbol-scoped read, test impact, and provenance
    - cg show returns one symbol body, not a file
    - Detect test files and add cg test-impact over refs
    - cg why joins symbol to commits, tasks, and memories
    - _Requirements: 2.3, 2.4, 2.5_

## Wave 2: git as a peer

- [ ] 2. Git integration
  - [x] 2.1 Ingest git history and rank by churn
    - Pipe git log to fill git_commits and git_churn tables
    - Use churn and recency as a ranking signal
    - cg commit --git mirrors the snapshot into a real git commit
    - _Requirements: 3.2, 3.3, 3.4_

## Wave 3: the plan lives in the tool

- [ ] 3. Spec authoring
  - [x] 3.1 Create, extend, and validate specs
    - cg spec new scaffolds a feature and workflow.kvx when absent
    - cg spec add inserts a task, preserving every other byte
    - cg spec lint reports cycles, unknown requires, missing ACs, dead globs
    - _Requirements: 4.1, 4.2, 4.3_
  - [x] 3.2 Parallel mode, leases, and the CI gate
    - Add parallel mode with claims, TTL leases, and touches-disjointness
    - cg spec wave lists every eligible task, not just the first
    - cg check gates render staleness, lint, and evidence in one command
    - _Requirements: 4.4, 4.5_

## Wave 5: present at every step

- [ ] 4. Ambient governance
  - [x] 4.1 Brief, review, guard, and hooks
    - cg brief returns session state in one call
    - cg review pairs changed symbols with acceptance criteria and at-risk callers
    - cg guard reports out-of-scope edits, warning unless --strict
    - cg hook install wires agent, git, and editor hooks to keep the graph fresh
    - _Requirements: 5.1, 5.2, 5.3, 5.4_
  - [x] 4.2 Memory maturity
    - Add --supersedes so reversed decisions stop surfacing first
    - Add cg memory compact to dedupe and decay outcome noise
    - Add recall --near <file> anchored retrieval
    - _Requirements: 5.5_

## Wave 6: speak what agents and editors speak

- [ ] 5. Protocols
  - [x] 5.1 Modernize the MCP surface
    - Add annotations and output schemas to every tool
    - Expose spec files and the task board as resources
    - Ship the workflow loop as prompts
    - Negotiate protocolVersion instead of echoing the client
    - Register every new command as a tool
    - _Requirements: 6.1, 6.2, 6.3_
  - [x] 5.2 Language server
    - Content-Length framed JSON-RPC lifecycle
    - definition, references, hover, and workspace symbol from the graph
    - Diagnostics for kvx errors, stale render, and out-of-scope edits
    - codeLens and codeAction over tasks and memories
    - _Requirements: 6.4, 6.5_

## Wave 8: tests and documentation

- [ ] 6. Proof
  - [x] 6.1 Tests and documentation
    - Integration coverage for every new command, including nested-root boundaries
    - Update README, ARCHITECTURE, and the command reference
    - _Requirements: 1.1, 6.5_
  - [x] 7.1 VS Code extension: language features and full workflow surface
    - Speak LSP to cg lsp with a dependency-free client
    - Definitions, references, hover, symbols, code lens, diagnostics
    - Task board with claims, parallel mode, and every spec action
    - Commands for brief, review, guard, check, why, test-impact, memory
    - kvx editing: navigate requires, complete keys, outline sections
    - _Requirements: 6.4_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 0,  "tasks": ["0.1", "0.2"] },
    { "id": 1,  "tasks": ["1.1", "1.2"] },
    { "id": 2,  "tasks": ["2.1"] },
    { "id": 3,  "tasks": ["3.1"] },
    { "id": 4,  "tasks": ["3.2"] },
    { "id": 5,  "tasks": ["4.1", "4.2"] },
    { "id": 6,  "tasks": ["5.1"] },
    { "id": 7,  "tasks": ["5.2"] },
    { "id": 8,  "tasks": ["6.1"] },
    { "id": 9,  "tasks": ["7.1"] }
  ]
}
```
