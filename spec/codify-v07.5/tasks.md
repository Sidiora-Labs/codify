<!-- GENERATED by cg spec render from spec/ kvx — DO NOT EDIT. -->

# Implementation Plan: codify-v07.5

## Tasks

## ACP and MCP

- [ ] 1. Protocol integration
  - [x] 1.1 Expose the full ACP session state
    - Advertise the supported client capabilities during initialize and keep the cg mcp injection explicit in session/new.
    - Map all stable ACP session update variants into panel messages, including command, mode, config, session-info, usage, and user-message updates.
    - Round-trip session/set_mode and session/set_config_option from controls in the webview, with inline failure feedback.
    - Extend the fake agent and protocol tests so pre-response session updates and interactive state changes cannot regress.
    - _Requirements: 1.1, 1.2, 1.3, 3.1_

## Responsive chat UI

- [ ] 2. Conversation experience
  - [x] 2.1 Make Markdown and live work easy to follow
    - Strengthen safe Markdown rendering for streaming task lists, nested content, external links, and code while keeping file links editor-native.
    - Add a compact activity summary for tool progress, plan progress, queued prompts, permissions, connection state, and context usage.
    - Reflow the context bar, transcript, cards, and composer for narrow sidebars and wider editor panels with accessible labels and reduced-motion support.
    - Extend DOM scenarios and document the visible behavior for users and adapter maintainers.
    - _Requirements: 2.1, 2.2, 2.3, 3.2, 3.3_

## Past sessions

- [ ] 3. Session continuity and install identity
  - [-] 3.1 Select and restore past ACP sessions
    - Persist compact workspace-scoped ACP session metadata and merge it with paginated session/list results without inventing unavailable history.
    - Add a past-session selector and refresh action, restoring through session/load or session/resume according to negotiated capabilities.
    - Keep cg mcp attached on restored sessions and surface adapter limitations or restore failures inline.
    - Show the installed extension version, use the authoritative SidioraLabs publisher identity, rebuild the current version, and prove the VSIX carries the current panel/controller.
    - _Requirements: 4.1, 4.2, 4.3, 5.1, 5.2_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1"] },
    { "id": 2,  "tasks": ["2.1"] },
    { "id": 3,  "tasks": ["3.1"] }
  ]
}
```
