<!-- GENERATED by cg spec render from spec/ kvx — DO NOT EDIT. -->

# Implementation Plan: Codify v0.4

## Tasks

## Wave 1: the graph tells the truth

- [ ] 1. Indexing accuracy
  - [x] 1.1 Scope-accurate extents and enclosing attribution
    - Compute real end_line per def: brace depth for brace languages, indentation for python, next-def fallback
    - Persist multi-line string state in clean_line (python triple quotes, js template literals)
    - Attribute refs to the innermost containing function/method scope; NULL outside any scope
    - Unit tests for scoping, string state, and attribution
    - _Requirements: 1.1, 1.2, 1.5_
  - [x] 1.2 Ref qualifiers, kinds, and the imports table
    - SymRef gains qual + kind; capture receiver tokens for qualified calls
    - Table-driven import extraction per language into the imports table
    - Writer inserts and purges imports alongside other child rows
    - _Requirements: 1.3, 1.4_
  - [x] 1.3 Schema v2 with survivable migration and content-hash skip
    - meta schema_version=2; on mismatch rebuild derived tables only — memories, leases, git data survive
    - Skip purge+reinsert when the recomputed content hash is unchanged
    - _Requirements: 1.6, 1.7_

## Wave 1: answers worth their tokens

- [ ] 2. Retrieval quality
  - [x] 2.1 Ranking overhaul and deterministic edges
    - find_symbols_all fuses exact and trigram tiers without the exact-match short-circuit
    - Score kinds, centrality, and churn; penalize test, fixture, and vendored paths
    - Import-aware, same-file-first resolution for callers and callees with stable ORDER BY
    - _Requirements: 2.1, 2.2, 2.5_
  - [x] 2.2 Budgeted, deduplicated context and capped reports
    - context --budget with proportional section slices, omitted counts, and a cross-section seen-set
    - Compact symbol form for repeats and edge lists; drop empty arrays
    - Cap changes and test-impact output; line numbers on full-text hits; show --full
    - Task-relevant memories and test-filtered entry points inside context
    - _Requirements: 2.3, 2.4_
  - [x] 2.3 MCP parameters and structured spec results
    - budget and limit parameters on context, impact, and changes tool schemas
    - Spec-mutating tools return JSON instead of prose
    - _Requirements: 2.6_

## Wave 1: the workflow carries the state

- [ ] 3. Sessions and parallel-safe dispatch
  - [x] 3.1 Handoff and resume
    - cg handoff stores a structured, superseding handoff memory against the task
    - cg resume returns task packet, latest handoff, task-scoped memories; --prompt renders a paste-ready block
    - handoff and resume MCP tools
    - _Requirements: 3.1, 3.2, 3.3, 3.4_
  - [x] 3.2 Per-agent identity and honest brief
    - Agent identity from flag or CG_AGENT; guard, review, brief, remember, and commit tagging follow the agent's task
    - brief surfaces task-scoped memories first with byte caps
    - _Requirements: 3.5, 4.6_
  - [x] 3.3 Lease integrity and honest gates
    - Atomic claims, owner-checked release, auto-release on done/implemented, live-lease conflict checks
    - flock around kvx read-modify-write
    - check catches partial glob overlaps and real trace evidence regressions
    - _Requirements: 4.1, 4.4, 4.5_
  - [x] 3.4 Ready frontier and atomic claim-next
    - cg spec ready lists every eligible task across waves
    - cg spec claim-next atomically picks, claims, and starts a disjoint task and returns the packet
    - spec_ready, spec_claim_next, spec_release MCP tools; ttl on claim
    - _Requirements: 4.2, 4.3_

## Wave 2: the plan runs itself

- [ ] 4. Orchestration
  - [x] 4.1 cg spec run driver loop
    - New module src/orchestrate.c: claim-next loop, fork/exec drivers, per-task logs, SIGINT cleanup
    - Verify task status on driver exit; release and record outcomes on failure; stop threshold
    - --dry-run prints the plan without claiming
    - _Requirements: 5.1, 5.2, 5.3, 5.4_
  - [x] 4.2 Codex and Claude drivers with workflow config
    - [agents] config: driver, cmd template, max, ttl, per-driver args
    - codex exec and claude -p templates with sandboxed defaults and stdin prompts
    - _Requirements: 5.5_

## Wave 1: the board drives the work

- [ ] 5. IDE agent sessions
  - [x] 5.1 Terminal-attached agent sessions
    - agents.js: claim task, seed terminal with the resume prompt, track terminal-to-claim mapping
    - Release offer on close; headless runs via the Tasks API
    - _Requirements: 6.1, 6.2_
  - [x] 5.2 Board freshness and handoff UX
    - graph.db watcher plus polling while sessions live; claim decorations on the board
    - handoff, resume, and run-wave commands from the IDE
    - _Requirements: 6.3, 6.4, 6.5_

## Wave 3: ship it

- [ ] 6. Docs and release
  - [x] 6.1 Documentation and changelog
    - README command reference and Driving agents section; ARCHITECTURE schema v2, sessions, orchestrator
    - CHANGELOG 0.4.0
    - _Requirements: 5.1, 6.1_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.2", "1.3", "2.1", "2.2", "2.3", "3.1", "3.2", "3.3", "3.4", "5.1", "5.2"] },
    { "id": 2,  "tasks": ["4.1", "4.2"] },
    { "id": 3,  "tasks": ["6.1"] }
  ]
}
```
